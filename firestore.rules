rules_version = '2';

// 各ユーザーは自分の users/{uid} 配下のみ読み書き可能
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{uid}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
